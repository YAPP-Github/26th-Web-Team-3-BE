version: "3.8"
services:
  green:
    image: jhzlo/yapp:dev
    container_name: green
    ports:
      - "8082:8080"
    env_file:
      - ../.env.dev
    environment:
      - TZ=Asia/Seoul
      - SPRING_PROFILES_ACTIVE=dev
    restart: always
    networks:
      - docker_compose_backend
    volumes:
      - /var/logs/dev:/app/logs/dev

networks:
  docker_compose_backend:
    external: true
